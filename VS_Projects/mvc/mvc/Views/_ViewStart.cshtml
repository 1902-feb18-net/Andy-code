@*
    We do views in ASP.NET mvc with CSHTML files
    that contains both Razor syntax and HTML
    the razor syntax is compiled with the view needs to be rendered into proper HTML
    razor comments looks like this
*@

@*
Below is a razor block
    it switches us entirely into C# mode to have statements separated by semicolons using curly braces
*@

@{
    // in here we write C# however we want
    Layout = "_Layout";
}
